import reducer from './reducer';

import selectionOperations from './operations';
import { types as selectionTypes } from './actions';

export {
  selectionOperations,
  selectionTypes
};

export default reducer;
